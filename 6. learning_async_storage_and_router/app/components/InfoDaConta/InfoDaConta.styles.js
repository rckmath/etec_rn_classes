import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  cardContainer: {
    backgroundColor: "#fff",
    padding: 40,
    borderRadius: 50,
    marginBottom: 20,
  },
  text: {
    fontSize: 26,
    marginBottom: 10,
  },
});

export default styles; 