import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  cardContainer: {
    padding: 40,
    borderRadius: 50,
    backgroundColor: "#fff",
    height: "50%",
  },
  helloWorldTitle: {
    fontWeight: 300,
    fontSize: 36,
    color: "#000",
  },
});

export default styles;
